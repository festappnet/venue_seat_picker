name: venue_seat_picker_example
publish_to: none

environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: ">=3.32.0"

dependencies:
  flutter:
    sdk: flutter
  venue_seat_picker:
    path: ..

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/venue.json
    - assets/venue_floor_plan.svg
    - assets/venue_zones.png
